<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.whiskyhunterapi">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WhiskyHunterApi">
        <activity android:name=".Modelos_main"></activity>
        <activity android:name=".anos_detalles" />
        <activity android:name=".Valor_class" />
        <activity android:name=".Destilleries_Details" />
        <activity android:name=".Auctions_Details" />
        <activity android:name=".Años_class" />
        <activity android:name=".Marcas" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>